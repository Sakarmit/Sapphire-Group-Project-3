class TableTemplate {
    static fillIn(tableID, dict, columnName) {
        let table = document.getElementById(tableID);

        // Fill in the header row
        let row = table.rows[0];
        for (let i = 0, cell; cell = row.cols[i]; i++) {
            if (cell.content == )
        }
    }
}